@model Group5__iCLOTHINGApp.Models.Brand

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Brand</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.brandName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.brandName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.brandDescription)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.brandDescription)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Product.productName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Product.productName)
        </dd>

    </dl>
</div>
<p>
    @if (Session["isAdminSS"] != null && Session["isAdminSS"] is bool)
    {
        if ((bool)Session["isAdminSS"])
        { // If isAdmin == true, display Edit button
            @Html.ActionLink("Edit", "Edit", new { id = Model.brandID }, new { @class = "btn btn-dark" })
        }
    }
    @Html.ActionLink("Return", "Index", new { area = "" }, new { @class = "btn btn-outline-dark" })
</p>
