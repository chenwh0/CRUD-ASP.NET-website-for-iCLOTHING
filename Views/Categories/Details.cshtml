@model Group5__iCLOTHINGApp.Models.Category

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.categoryName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.categoryName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.categoryDescription)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.categoryDescription)
        </dd>

    </dl>
</div>

<p>
    @if (Session["isAdminSS"] != null && Session["isAdminSS"] is bool)
    {
        if ((bool)Session["isAdminSS"])
        { // If isAdmin == true, display Edit button
            @Html.ActionLink("Edit", "Edit", new { id = Model.categoryID }, new { @class = "btn btn-dark" })
        }
    }
    @Html.ActionLink("Return", "Index", new { area = "" }, new { @class = "btn btn-outline-dark" })
</p>
