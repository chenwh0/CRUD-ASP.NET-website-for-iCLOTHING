@model Group5__iCLOTHINGApp.Models.Department

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Department</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.departmentName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.departmentName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.departmentDescription)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.departmentDescription)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Cataloging.categoryID)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Cataloging.categoryID)
        </dd>

    </dl>
</div>

<p>
    @if (Session["isAdminSS"] != null && Session["isAdminSS"] is bool)
    {
        if ((bool)Session["isAdminSS"])
        { // If isAdmin == true, display Edit button
            @Html.ActionLink("Edit", "Edit", new { id = Model.departmentID }, new { @class = "btn btn-dark" })
        }
    }
    @Html.ActionLink("Return", "Index", new {area=""}, new { @class = "btn btn-outline-dark" })
</p>
